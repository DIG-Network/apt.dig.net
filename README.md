# apt.dig.net
DIG APT repository + AWS infra — apt install the DIG ecosystem on Ubuntu/Debian; .deb packaging (digstore CLI, dig-node) + dig-node systemd service; signed apt repo served at apt.dig.net
